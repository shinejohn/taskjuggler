[phases.setup]
nixPkgs = ['php82', 'php82Packages.composer', 'php82Extensions.pdo_pgsql', 'postgresql', 'nodejs_20']

[phases.install]
cmds = [
  'composer install --no-interaction --optimize-autoloader --no-dev --prefer-dist --no-scripts'
]

[phases.build]
cmds = [
  'php artisan package:discover --ansi',
  'php artisan config:cache',
  'php artisan route:cache',
  'php artisan view:cache'
]

[start]
cmd = 'if [ "$NIGHTWATCH_ENABLED" = "true" ]; then php artisan nightwatch:agent & fi; php artisan migrate --force --no-interaction || true && php artisan storage:link --force || true && php artisan serve --host=0.0.0.0 --port=${PORT:-8000}'

[variables]
COMPOSER_MEMORY_LIMIT = '-1'
