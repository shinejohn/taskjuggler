{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "nixpacksConfigPath": "nixpacks.toml"
    },
    "deploy": {
        "startCommand": "if [ \"$NIGHTWATCH_ENABLED\" = \"true\" ]; then php artisan nightwatch:agent & fi; php artisan schedule:work",
        "numReplicas": 1,
        "restartPolicyType": "ALWAYS"
    }
}
